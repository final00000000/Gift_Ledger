# 活动簿方案执行计划

## 背景
- 现有功能支持单条随礼记录
- 目标新增可自定义活动簿与批量收礼
- 范围限定模型、数据库、服务层与统计配置，不包含 UI

## 计划
1. 新增 `EventBook` 模型与序列化
2. `Gift` 增加 `eventBookId` 关联字段
3. 数据库新增 `event_books` 与迁移逻辑
4. 数据服务层扩展 CRUD 与批量写入
5. 统计逻辑支持包含/排除活动簿
6. 配置项持久化统计范围
